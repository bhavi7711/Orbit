# Copy this file to .env in the same directory (packages/server/.env).
# .env is gitignored — NEVER commit the real key.
#
# Get the actual GEMINI_API_KEY from Preethesh privately (WhatsApp/DM).
# Note: the current key was issued 2026-07-11 and expires ~24h later —
# if agents return 403, ask Preethesh for a fresh key or create one at
# https://aistudio.google.com
GEMINI_API_KEY=your-key-here
